=TRIM(RIGHT(LEFT(A1,LEN(A1)-LEN(SUBSTITUTE(A1,"/",""))+1),LEN(LEFT(A1,LEN(A1)-LEN(SUBSTITUTE(A1,"/",""))+1))-(FIND("/",LEFT(A1,LEN(A1)-LEN(SUBSTITUTE(A1,"/",""))+1))-1)))
=MID(A1,FIND("/",A1,FIND("/",A1)+1)+1,FIND("/",A1,FIND("/",A1,FIND("/",A1)+1)+1)-FIND("/",A1,FIND("/",A1)+1)-1)
